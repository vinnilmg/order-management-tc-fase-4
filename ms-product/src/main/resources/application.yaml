spring:
  sql:
    init:
      mode: always

  application:
    name: ms-product

  datasource:
    driver-class-name: org.h2.Driver
    jdbc-url: jdbc:h2:mem:testdb
    username: root
    password: root

  batch:
    job:
      enabled: false
    initialize-schema: always

  transaction:
    management:
      enabled: true


  h2:
    console:
      enabled: true

  jpa:
    show: true
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
      show_sql: true

server:
  port: 8092

logging:
  level:
    root: INFO
    hibernate: TRACE
    org.springframework.transaction: DEBUG

directory:
  pending: /home/lobaoff/Desktop/repositories/order-management-tc-fase-4/ms-product/src/main/resources/imports/pending
  finished: /home/lobaoff/Desktop/repositories/order-management-tc-fase-4/ms-product/src/main/resources/imports/finished
  waiting: /home/lobaoff/Desktop/repositories/order-management-tc-fase-4/ms-product/src/main/resources/imports/waiting


